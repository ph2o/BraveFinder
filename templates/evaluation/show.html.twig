{% extends 'base.html.twig' %}

{% block title %}Evaluation
{% endblock %}

{% block body %}
	<h1>Evaluation</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ evaluation.id }}</td>
			</tr>
			<tr>
				<th>Rate</th>
				<td>{{ evaluation.rate }}</td>
			</tr>
			<tr>
				<th>Comment</th>
				<td>{{ evaluation.comment }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('evaluation.index') }}">back to list</a>

	<a href="{{ path('evaluation.edit', {'id': evaluation.id}) }}">edit</a>

	{{ include('evaluation/_delete_form.html.twig') }}
{% endblock %}
